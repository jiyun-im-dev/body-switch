# IDE 관련 설정 파일 (IntelliJ, Eclipse, VSCode)
.idea/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/
.vscode/

# 빌드 결과물
# ./gradlew build 실행 시 생성되는 폴더로, 컴파일된 클래스 파일과 JAR/WAR 파일이 포함됩니다.
build/
out/

# Gradle 관련 캐시 폴더
# 빌드 속도를 높이기 위해 Gradle이 사용하는 캐시 및 래퍼 파일입니다.
.gradle/

# 운영체제 관련 파일
.DS_Store
Thumbs.db

# 로그 파일
*.log

# 로컬 환경 설정 파일 (선택 사항이지만 강력 추천)
# 로컬 DB 비밀번호 등 민감한 정보를 담는 파일로, 공유하면 안 됩니다.
application-local.properties
application-local.yml
application-local.yaml